
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to all files for any user
    // during development.
    // WARNING: Secure these rules before deploying to production!
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
